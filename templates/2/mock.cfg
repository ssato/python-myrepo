config_opts = {}
execfile('{{ base_mock_cfg_path }}')

config_opts['root'] = '{{ repo.name }}-{{ dist.label }}'
config_opts['myrepo_distname'] = '{{ dist.name }}'
config_opts['yum.conf'] += """
{{ release_file_content }}
"""
